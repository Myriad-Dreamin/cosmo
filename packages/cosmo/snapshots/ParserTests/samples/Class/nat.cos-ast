Some(
  value = Block(
    stmts = List(
      Class(
        name = "Nat",
        params = None,
        body = CaseBlock(
          stmts = List(
            Case(cond = Ident(name = "Zero"), body = None),
            Case(
              cond = Apply(lhs = Ident(name = "Succ"), rhs = List(Ident(name = "Nat"))),
              body = None
            )
          )
        )
      )
    )
  )
)