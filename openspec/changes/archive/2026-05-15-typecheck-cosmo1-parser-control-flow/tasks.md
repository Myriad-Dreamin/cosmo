## 1. Control Flow Rules

- [x] 1.1 Type-check `if` and `else` expressions.
- [x] 1.2 Type-check `while` loops as parser-source control flow.
- [x] 1.3 Track nested block scopes and early returns.

## 2. Return Compatibility

- [x] 2.1 Validate function and method body return compatibility.
- [x] 2.2 Add conservative missing-return diagnostics for parser-source functions.

## 3. Validation

- [x] 3.1 Add full `packages/cosmoc/src/parser.cos` type-check fixture.
- [x] 3.2 Add negative fixtures for bad conditions and return mismatches.
