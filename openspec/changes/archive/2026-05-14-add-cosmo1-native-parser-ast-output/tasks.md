## 1. Parser Output Model

- [x] 1.1 Define the initial syntax node kinds required by `parser.cos`.
- [x] 1.2 Add parser result data that carries a syntax root and diagnostics.

## 2. Parser Implementation

- [x] 2.1 Parse top-level class, function, type alias, value, and class member forms into syntax nodes.
- [x] 2.2 Parse parser-body expressions into syntax nodes for blocks, calls, selection, assignment, conditionals, loops, and returns.

## 3. Validation

- [x] 3.1 Add parser fixtures proving the supported `parser.cos` subset produces syntax.
- [x] 3.2 Add negative fixtures for unsupported full-language constructs.
