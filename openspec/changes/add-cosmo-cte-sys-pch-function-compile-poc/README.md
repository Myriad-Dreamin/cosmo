# add-cosmo-cte-sys-pch-function-compile-poc

Add a native compile-time execution support component that compiles provider
entry functions with Clang and uses PCH/precompiled context caching instead of
clang-repl or clangInterpreter.
