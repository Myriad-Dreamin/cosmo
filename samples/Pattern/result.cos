
@noCore();

class Result[T, E] {
  case Ok(T);
  case Err(E);
}

def main() = {
  val z = Result(i32, i32)::Ok(42);
  val w = Result(i32, str)::Err("error");
  // println(x.unwrap());
  // println(y.is_none());
  // println(z.unwrap());
  // println(w.unwrap_err());
}
